# Claude Code 2.1.3 patch configuration
# Variable names in minified cli.js for this version

# Modes array variable name
MODES_ARRAY_VAR="QP"

# Mode variable in setMode handler
MODE_VAR="S0"

# Telemetry function name
TELEMETRY_FN="y9"

# Delegate check functions (note: $k0 needs escaping)
DELEGATE_FN1="\$k0"
DELEGATE_FN2="PmA"

# Permission context variable (for PA.mode pattern)
PERMISSION_CTX_VAR="PA"
