# Claude Code 2.1.5 patch configuration
# Generated: 2026-01-12
#
# These variables are extracted from the minified cli.js and change between versions.
# The patch-claude.sh script uses these to apply the auto mode patches.

# Variable holding the modes array: ["acceptEdits","bypassPermissions","default","delegate","dontAsk","plan"]
MODES_ARRAY_VAR="QP"

# Permission context variable used in .mode==="bypassPermissions"||V) checks
PERMISSION_CTX_VAR="PA"

# Mode variable used in if(MODE_VAR==="acceptEdits") telemetry check
MODE_VAR="S0"

# Telemetry function called for auto-accept-mode
TELEMETRY_FN="y9"

# Delegate functions called when mode changes
DELEGATE_FN1="Ok0"
DELEGATE_FN2="SmA"
