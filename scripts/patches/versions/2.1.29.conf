# Claude Code 2.1.29 patch configuration
# Generated: 2026-02-02
#
# These variables are extracted from the minified cli.js and change between versions.
# The patch-claude.sh script uses these to apply the auto mode patches.

# Variable holding the modes array: ["acceptEdits","bypassPermissions","default","delegate","dontAsk","plan"]
MODES_ARRAY_VAR="S11"

# Permission context variable used in .mode==="bypassPermissions"||BOOL) checks
PERMISSION_CTX_VAR="Z6"

# Boolean variable in permission checks
PERMISSION_BOOL_VAR="D"

# Mode variable used in if(MODE_VAR==="acceptEdits") telemetry check
MODE_VAR="C6"

# Telemetry function called for auto-accept-mode
TELEMETRY_FN="Q4"

# Delegate functions called when mode changes
DELEGATE_FN1="lT6"
DELEGATE_FN2="iZ1"

# Patch 5a: Main permission context variable (toolPermissionContext)
TOOL_PERMISSION_CTX="z"

# Patch 5b: Passthrough mode check variable
PASSTHROUGH_MODE_VAR="q"

# Patch 7b: Mode context variable used in delegate check
DELEGATE_MODE_CTX="K"
