{
  "description": "Supercompact PreCompact hook - runs EITF entity-preservation compaction before Claude's default compaction",
  "hooks": {
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks-handlers/supercompact-precompact.sh"
          }
        ]
      }
    ]
  }
}
