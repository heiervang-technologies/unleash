name: Docker Publish

# Builds and pushes the Docker image to Docker Hub on release and manual trigger.
# Requires repo secrets: DOCKER_PAT
#
# Pull requests trigger a *dry-run*: the image is built and the CLIs are
# verified, but the login and push steps are skipped. This catches problems
# in the publish path (e.g. tag format, build failures, CLI install regressions)
# at PR time rather than after merge to main.

on:
  release:
    types: [published]
  push:
    branches: [main]
    paths:
      - 'docker/**'
      - 'src/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'plugins/**'
  pull_request:
    branches: [main]
    paths:
      - 'docker/**'
      - 'src/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'build.rs'
      - 'plugins/**'
      - 'scripts/**'
      - '.github/workflows/docker-publish.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    name: Build & Push to Docker Hub
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Log in to Docker Hub
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4
        with:
          username: marksverdhei
          password: ${{ secrets.DOCKER_PAT }}

      - name: Extract metadata
        id: meta
        run: |
          VERSION=$(grep '^version' Cargo.toml | head -1 | sed 's/.*"\(.*\)"/\1/')
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

          # Tags: latest + version on release, just latest on push to main
          if [[ "${{ github.event_name }}" == "release" ]]; then
            echo "tags=marksverdhei/unleash:latest,marksverdhei/unleash:v${VERSION}" >> $GITHUB_OUTPUT
          else
            echo "tags=marksverdhei/unleash:latest" >> $GITHUB_OUTPUT
          fi

      - name: Build Docker image
        run: |
          docker build -f docker/Dockerfile \
            --label "org.opencontainers.image.version=${{ steps.meta.outputs.version }}" \
            --label "org.opencontainers.image.source=https://github.com/heiervang-technologies/unleash" \
            -t unleash:local .

      - name: Verify all CLIs
        run: |
          FAILED=0

          for cli in claude codex gemini opencode pi; do
            echo -n "  $cli: "
            VERSION=$(docker run --rm unleash:local $cli --version 2>&1 | grep -iE '[0-9]+\.[0-9]+' | head -1) || true
            if [ -n "$VERSION" ]; then
              echo "$VERSION"
            else
              echo "FAILED"
              docker run --rm unleash:local $cli --version 2>&1 || true
              FAILED=1
            fi
          done

          # gh needs --entrypoint since the default entrypoint is unleash
          echo -n "  gh: "
          VERSION=$(docker run --rm --entrypoint gh unleash:local --version 2>&1 | grep -iE '[0-9]+\.[0-9]+' | head -1) || true
          if [ -n "$VERSION" ]; then
            echo "$VERSION"
          else
            echo "FAILED"
            docker run --rm --entrypoint gh unleash:local --version 2>&1 || true
            FAILED=1
          fi

          # Entrypoint is already 'unleash', so just pass --version directly
          echo -n "  unleash: "
          VERSION=$(docker run --rm unleash:local --version 2>&1 | grep -iE '[0-9]+\.[0-9]+' | head -1) || true
          if [ -n "$VERSION" ]; then
            echo "$VERSION"
          else
            echo "FAILED"
            docker run --rm unleash:local --version 2>&1 || true
            FAILED=1
          fi

          if [ "$FAILED" -eq 1 ]; then
            echo "CLI verification failed — not pushing"
            exit 1
          fi

      - name: Build CUDA variant
        run: |
          docker build -f docker/Dockerfile.cuda \
            --label "org.opencontainers.image.version=${{ steps.meta.outputs.version }}" \
            --label "org.opencontainers.image.source=https://github.com/heiervang-technologies/unleash" \
            -t unleash:cuda-local .

      - name: Verify CUDA variant CLIs
        run: |
          FAILED=0

          for cli in claude codex gemini opencode pi; do
            echo -n "  $cli: "
            VERSION=$(docker run --rm unleash:cuda-local $cli --version 2>&1 | grep -iE '[0-9]+\.[0-9]+' | head -1) || true
            if [ -n "$VERSION" ]; then
              echo "$VERSION"
            else
              echo "FAILED"
              docker run --rm unleash:cuda-local $cli --version 2>&1 || true
              FAILED=1
            fi
          done

          echo -n "  PyTorch: "
          PYTORCH_VERSION=$(docker run --rm unleash:cuda-local python3 -c "import torch; print(torch.__version__)" 2>&1) || true
          if [ -n "$PYTORCH_VERSION" ]; then
            echo "$PYTORCH_VERSION"
          else
            echo "FAILED"
            FAILED=1
          fi

          echo -n "  unleash: "
          VERSION=$(docker run --rm unleash:cuda-local --version 2>&1 | grep -iE '[0-9]+\.[0-9]+' | head -1) || true
          if [ -n "$VERSION" ]; then
            echo "$VERSION"
          else
            echo "FAILED"
            docker run --rm unleash:cuda-local --version 2>&1 || true
            FAILED=1
          fi

          if [ "$FAILED" -eq 1 ]; then
            echo "CUDA variant CLI verification failed — not pushing cuda tag"
            exit 1
          fi

      - name: Tag and push
        if: github.event_name != 'pull_request'
        env:
          TAGS: ${{ steps.meta.outputs.tags }}
        run: |
          IFS=',' read -ra TAG_ARR <<< "$TAGS"
          for tag in "${TAG_ARR[@]}"; do
            docker tag unleash:local "$tag"
            docker push "$tag"
            echo "Pushed: $tag"

            # Also push CUDA variant with +cuda suffix
            CUDA_TAG="${tag}-cuda"
            docker tag unleash:cuda-local "$CUDA_TAG"
            docker push "$CUDA_TAG"
            echo "Pushed: $CUDA_TAG"
          done

          # Push standalone :cuda tag for the CUDA variant
          if [[ "${{ github.event_name }}" == "release" ]]; then
            VERSION="${{ steps.meta.outputs.version }}"
            for T in "marksverdhei/unleash:cuda" "marksverdhei/unleash:v${VERSION}-cuda"; do
              docker tag unleash:cuda-local "$T"
              docker push "$T"
              echo "Pushed: $T"
            done
          else
            docker tag unleash:cuda-local "marksverdhei/unleash:cuda"
            docker push "marksverdhei/unleash:cuda"
            echo "Pushed: marksverdhei/unleash:cuda"
          fi

      - name: Summary
        env:
          VERSION: ${{ steps.meta.outputs.version }}
          TAGS: ${{ steps.meta.outputs.tags }}
          EVENT: ${{ github.event_name }}
        run: |
          if [ "$EVENT" = "pull_request" ]; then
            {
              echo "## Docker Publish (dry-run)"
              echo "- Version: $VERSION"
              echo "- Would-be tags: $TAGS + cuda variants"
              echo "- Push skipped: pull_request event"
            } >> "$GITHUB_STEP_SUMMARY"
          else
            {
              echo "## Docker Image Published"
              echo "- Version: $VERSION"
              echo "- Tags: $TAGS"
              echo "- CUDA tags: marksverdhei/unleash:cuda, marksverdhei/unleash:v${VERSION}-cuda"
            } >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Cleanup
        if: always()
        run: |
          docker rmi unleash:local unleash:cuda-local 2>/dev/null || true
          docker builder prune -f --filter "until=1h" 2>/dev/null || true
